@model BrowseViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div style="display:grid; grid-template-columns: auto auto auto auto; justify-content: space-evenly;">

    @foreach (var route in Model.Routes)
    {
        <div>
            <a class="nav-link text-light" style="display:inline" asp-area="" asp-controller="Route" asp-action="Show" asp-route-id="@route.RouteReference">
                <div class="card bg-secondary mb-3" style="max-width: 20rem;">
                <div class="card-header">@route.RouteReference</div>
                    <div class="card-body">

                    <h4 class="card-title">@route.Description</h4>
                    <img src="@route.Image" class="d-block user-select-none" width="100%" height="200" aria-label="Placeholder: Image cap" focusable="false" role="img" preserveAspectRatio="xMidYMid slice" viewBox="0 0 318 180" style="font-size:1.125rem;text-anchor:middle">
                    </div>
                </div>
            </a>
        </div>
    }

 </div>

